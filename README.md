# Uzair Portfolio Monorepo

This repository contains the Headless CMS architecture for Uzair's Portfolio.

## Architecture

- `/frontend` - Next.js (App Router) containing the Public Site and the React Admin.
- `/backend` - Laravel API (Enterprise Architecture).

## Execution Orders
Strictly follow the Module Execution Order defined in `docs/backend/`.
